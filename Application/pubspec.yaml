name: smart_security_band_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  google_maps_flutter: ^2.5.3
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  url_launcher: ^6.3.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  vibration: ^2.1.0
  audioplayers: ^5.2.1
  flutter_blue_plus: ^1.31.0
  geocoding: ^3.0.0
  provider: ^6.1.1
  lottie: ^3.1.0
  flutter_animate: ^4.5.0
  firebase_core: ^3.10.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.3
  local_auth: ^2.2.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
